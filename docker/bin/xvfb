#!/bin/bash
if [ -z "$1" ]; then
  echo "`basename $0` {start|stop}"
  exit
fi

case "$1" in
start)
  /usr/bin/Xvfb :15 -ac -screen 0 1024x768x8 &
  export DISPLAY=:15
;;
stop)
  killall Xvfb
;;
restart)
  $0 stop
  $0 start
;;
esac

exit 0
