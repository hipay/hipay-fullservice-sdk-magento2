<?php

/*
 * HiPay fullservice SDK
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Apache 2.0 Licence
 * that is bundled with this package in the file LICENSE.md.
 * It is also available through the world-wide-web at this URL:
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * @copyright      Copyright (c) 2016 - HiPay
 * @license        http://www.apache.org/licenses/LICENSE-2.0 Apache 2.0 Licence
 *
 */

// @codingStandardsIgnoreFile
/**
 * @var \HiPay\FullserviceMagento\Block\Redirect\PendingPolling $block
 */
?>
<?=  $block
        ->getLayout()
        ->createBlock('HiPay\FullserviceMagento\Block\ExternalJS')
        ->setTemplate('HiPay_FullserviceMagento::additional/external_js.phtml')
        ->toHtml()
?>
<div class="page-title">
    <h1><?= /* @escapeNotVerified */ __('Your payment is in pending.') ?></h1>
</div>

<?php if ($block->getRealOrderId()): ?><p>
    <?= /* @escapeNotVerified */ __('Order #') . $block->getRealOrderId() ?></p><?php endif ?>
<?php if ($error = $block->getErrorMessage()): ?><p><?= /* @escapeNotVerified */ $error ?></p><?php endif ?>
<p><?= /* @escapeNotVerified */ __('Click <a href="%1">here</a> to continue shopping.', $escaper->escapeUrl($block->getContinueShoppingUrl())) ?>
</p>
<script type="text/javascript">
    require(['jquery'], function ($) {
        var attempts = 0;
        var maxAttempts = <?= (int) $block->getPollingMaxAttempts(); ?>;
        var pendingUrl = '<?= $escaper->escapeUrl($block->getPendingUrl()); ?>';
        var declineUrl = '<?= $escaper->escapeUrl($block->getDeclineUrl()); ?>';
        var placeOrderStatusUrl = '<?= $escaper->escapeUrl($block->getPlaceOrderStatusUrl()); ?>';

        function redirectToFailure() {
            clearInterval(interval);
            window.location.assign(declineUrl);
        }

        var interval = setInterval(function () {
            attempts++;

            $.get(placeOrderStatusUrl)
                .done(function (response) {
                    if (
                        response &&
                        response.redirectUrl &&
                        response.redirectUrl !== pendingUrl
                    ) {
                        clearInterval(interval);
                        window.location.assign(response.redirectUrl);
                        return;
                    }

                    if (attempts >= maxAttempts) {
                        redirectToFailure();
                    }
                })
                .fail(function () {
                    if (attempts >= maxAttempts) {
                        redirectToFailure();
                    }
                });
        }, <?= (int) $block->getPollingInterval(); ?>);
    });
</script>
